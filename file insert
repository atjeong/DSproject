#include <stdio.h>
#include <stdlib.h>

int main()
{   int i,ch;
    FILE * user = fopen("user.txt","rt");

    if(user == NULL){
        printf("notcomplete!");
        return -1;
    }
    for(i=0; i<3; i++){
        ch = fgetc(user);
        printf("%c \n", ch);
    }

    return 0;
}
